import React from 'react';

import { Typography }  from '@mui/material';

const AnotherTitle = (props) => {

    return (

        <Typography>
            {props.title}
        </Typography>

    )
}

export {
    AnotherTitle
}